{
  "name": "testbrowserextension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:release": "node build.mjs --bump-version",
    "watch": "node build.mjs --watch",
    "clean": "rm -rf dist",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.3"
  }
}
